---
phase: 06-feed-distance-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - tests/test_bmcu_feeder.py
  - tests/test_protocol.py
autonomous: true
requirements:
  - D-06
  - D-07

must_haves:
  truths:
    - "Pytest verifies feed_mm_since_reset accumulates across multiple STATUS polls"
    - "Pytest verifies feed_mm_since_reset is zero when mm= values are constant"
    - "Hardware test verifies mm= values advance continuously during motor run"
  artifacts:
    - path: "tests/test_bmcu_feeder.py"
      provides: "TestFeedAccumulation class with accumulation and stale-value tests"
      contains: "class TestFeedAccumulation"
    - path: "tests/test_protocol.py"
      provides: "test_feed_continuous_accumulation method in BMCUTester"
      contains: "def test_feed_continuous_accumulation"
  key_links:
    - from: "tests/test_bmcu_feeder.py:TestFeedAccumulation"
      to: "klippy/extras/bmcu_feeder.py:BmcuFeeder"
      via: "_poll_status with injected STATUS lines"
      pattern: "_poll_status"
    - from: "tests/test_protocol.py:test_feed_continuous_accumulation"
      to: "firmware UART protocol"
      via: "serial send_recv STATUS"
      pattern: "send_recv.*STATUS"
---

<objective>
Add pytest unit tests for Klipper-side feed_mm accumulation and a hardware integration test for continuous firmware-side accumulation.

Purpose: Unit tests verify that the Klipper extra correctly tracks feed_mm_since_reset across multiple STATUS polls (offline, no hardware needed). The hardware test validates that the firmware fix from Plan 01 actually produces continuously advancing mm= values during motor runs — the end-to-end proof that the bug is fixed.

Output: TestFeedAccumulation pytest class; test_feed_continuous_accumulation hardware test method.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-feed-distance-tracking/06-CONTEXT.md
@.planning/phases/06-feed-distance-tracking/06-RESEARCH.md
@.planning/phases/06-feed-distance-tracking/06-PATTERNS.md
@.planning/phases/06-feed-distance-tracking/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TestFeedAccumulation pytest class</name>
  <files>tests/test_bmcu_feeder.py</files>
  <read_first>
    tests/test_bmcu_feeder.py
    tests/conftest.py
    klippy/extras/bmcu_feeder.py (get_status() method and _dispatch_status_line() for feed_mm field paths)
  </read_first>
  <action>
Add a new TestFeedAccumulation class to tests/test_bmcu_feeder.py (per D-06). Place it after the existing TestBmcuDiagnostics class, near the end of the file.

The class needs two helpers:
- `_make_feeder_with_channels(self, monkeypatch, ch_ids=(0,))` — copy the pattern from TestBmcuPolling (lines 426-447): create MockConfig with serial=_VALID_SERIAL, instantiate BmcuFeeder, create BmcuChannel for each ch_id, monkeypatch serial.Serial with a lambda that creates MockSerial(port, baud, timeout), call feeder._handle_connect(), return (feeder, serial_instances).
- `_dispatch(self, feeder, ch_id, feed_mm, ...)` — copy the pattern from TestBmcuDiagnostics (lines 1216-1223): inject STATUS line into feeder._serial._lines and call feeder._poll_status(0.0).

Required test methods (minimum):

1. `test_accumulates_across_polls(self, monkeypatch)` — inject mm=10.0 (init), mm=20.0, mm=30.0 for ch=0. Assert feed_mm_since_reset equals 20.0 (30.0 - 10.0 init snapshot). Access via `feeder.get_status(0.0)['channels']['0']['feed_mm_since_reset']`.

2. `test_mm_stable_shows_zero_delta(self, monkeypatch)` — inject mm=50.0 (init), mm=50.0, mm=50.0. Assert feed_mm_since_reset equals 0.0. This documents the old-bug scenario where firmware didn't accumulate between polls.

3. `test_negative_mm_accumulation(self, monkeypatch)` — inject mm=100.0 (init), mm=80.0. Assert feed_mm_since_reset equals approximately -20.0 (signed per STATE.md decision: feed_mm_since_reset is signed).

4. `test_reset_clears_accumulation(self, monkeypatch)` — inject mm=10.0 (init), mm=50.0, then call feeder._cmd_reset_feed(MockGcmd({})), inject mm=60.0. Assert feed_mm_since_reset equals 10.0 (60.0 - 50.0 reset point).

All mm= values in tests use the placeholder GEAR_CIRCUMFERENCE_MM=30.0f constant implicitly (firmware computes mm from counts). Since these tests inject mm= values directly into STATUS lines, the constant does not affect the assertions — document this in a class docstring.
  </action>
  <verify>
    <automated>cd /home/kurt/klipper-bmcu-libre && python3 -m pytest tests/test_bmcu_feeder.py::TestFeedAccumulation -x -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_bmcu_feeder.py contains `class TestFeedAccumulation`
    - Class has at least 4 test methods: test_accumulates_across_polls, test_mm_stable_shows_zero_delta, test_negative_mm_accumulation, test_reset_clears_accumulation
    - All TestFeedAccumulation tests pass: `python3 -m pytest tests/test_bmcu_feeder.py::TestFeedAccumulation -x` exits 0
    - Full suite still passes: `python3 -m pytest tests/test_bmcu_feeder.py -q` shows no new failures
    - Tests use monkeypatch for serial.Serial (not direct MockSerial construction in test body)
    - Assertions use pytest.approx() for float comparisons
  </acceptance_criteria>
  <done>TestFeedAccumulation class with 4+ tests all passing; full suite green</done>
</task>

<task type="auto">
  <name>Task 2: Add hardware test for continuous feed accumulation</name>
  <files>tests/test_protocol.py</files>
  <read_first>
    tests/test_protocol.py
    .planning/phases/06-feed-distance-tracking/06-RESEARCH.md (Pattern 5: Hardware Test for Continuous Accumulation)
  </read_first>
  <action>
Add test_feed_continuous_accumulation method to BMCUTester class in tests/test_protocol.py (per D-07).

Method implementation:
1. Print header: `\n[test_feed_continuous_accumulation] Phase 6`
2. Send "ENABLE" via self.send_recv() — required per Pitfall 4 since test cannot rely on prior ENABLE
3. Send "RUN 0" via self.send_recv()
4. Loop 6 times with 0.5s sleep between iterations: send "STATUS", parse mm= value for ch=0 using `re.search(r"ch=0 [^\n]*mm=(-?[\d.]+)", resp)`, append float to mm_values list
5. Send "STOP 0" via self.send_recv()
6. Assert at least 3 samples collected: `self.assert_true("At least 3 STATUS samples", len(mm_values) >= 3)`
7. Assert each consecutive mm value is strictly greater than the previous (motor running FWD should produce monotonically increasing mm=): use `all(mm_values[i+1] > mm_values[i] for i in range(len(mm_values)-1))`
8. Call self.assert_true with the values list in the detail string for debugging

Also append `t.test_feed_continuous_accumulation` to the `tests` list in `main()` (after `t.test_feed_distance`).

Import time at the top of the file if not already imported (it likely is — verify in read_first).
  </action>
  <verify>
    <automated>cd /home/kurt/klipper-bmcu-libre && python3 -c "import ast; tree = ast.parse(open('tests/test_protocol.py').read()); cls = [n for n in ast.walk(tree) if isinstance(n, ast.ClassDef) and n.name == 'BMCUTester'][0]; methods = [n.name for n in cls.body if isinstance(n, ast.FunctionDef)]; assert 'test_feed_continuous_accumulation' in methods, f'Missing method. Found: {methods}'; print('OK: method exists')"</automated>
  </verify>
  <acceptance_criteria>
    - BMCUTester class contains `def test_feed_continuous_accumulation(self)` method
    - Method sends ENABLE before RUN 0
    - Method polls STATUS at least 6 times with sleep intervals
    - Method parses mm= from ch=0 using regex
    - Method asserts monotonically increasing mm= values
    - Method sends STOP 0 after polling loop
    - `test_feed_continuous_accumulation` appears in the tests list in main()
    - File parses without syntax errors: `python3 -c "import ast; ast.parse(open('tests/test_protocol.py').read())"` exits 0
  </acceptance_criteria>
  <done>test_feed_continuous_accumulation method added to BMCUTester and registered in main() tests list; requires physical BMCU for execution</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries changed. Test files do not run in production. The hardware test communicates over USB serial to a locally-connected BMCU — no network exposure.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | N/A | Test infrastructure | accept | Test code only; no production impact |

No packages installed. No supply chain threats.
</threat_model>

<verification>
- `python3 -m pytest tests/test_bmcu_feeder.py::TestFeedAccumulation -v` — all tests pass
- `python3 -m pytest tests/test_bmcu_feeder.py -q` — full suite green (62+ tests)
- `python3 -c "import ast; ast.parse(open('tests/test_protocol.py').read())"` — no syntax errors
- `grep -c "test_feed_continuous_accumulation" tests/test_protocol.py` returns at least 2 (method def + tests list entry)
</verification>

<success_criteria>
- TestFeedAccumulation pytest class with 4+ passing tests covering accumulation, stale values, negative delta, and reset
- Hardware test method registered and syntactically valid (execution requires physical BMCU)
- Full pytest suite passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/06-feed-distance-tracking/06-02-SUMMARY.md` when done
</output>
